import React from 'react';

// This component is no longer used and its functionality has been removed.
const ProjectDetailModal: React.FC<{
  project: any;
  onUpdateProject: (updatedProject: any) => void;
  onClose: () => void;
}> = () => {
  return null;
};

export default ProjectDetailModal;
